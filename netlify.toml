# Netlify 部署配置（静态站点，无需构建）
[build]
  publish = "."
  command = "echo 'static site, no build step'"

# 缓存静态资源
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"
